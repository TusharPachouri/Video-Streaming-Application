import mongoose from 'mongoose';

const subscriberSchema = new mongoose.Schema({

})

export const Subscriber = mongoose.model('Subscriber', subscriberSchema)

// write a code for writing number table